# Homebrew Tap

Homebrew formulae for projects maintained by Filip Pytloun.

## Usage

```bash
brew tap fpytloun/tap
brew install cognis-executor
```

The `cognis-executor` formula will be added with its first public macOS release.
Installation and configuration instructions are maintained in the Cognis
repository.
